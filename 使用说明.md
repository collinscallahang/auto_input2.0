# 货拉拉报价工具使用说明

## 首次使用

1. 从 GitHub Release 页面下载 `auto_input2.0.exe`。
2. 把 `auto_input2.0.exe` 放到电脑任意文件夹，例如桌面。
3. 双击打开 `auto_input2.0.exe`。
4. 点击 `选择 Excel`，选择要报价的 `.xlsx` 文件。
5. 在 `字段识别预览` 里确认 `供应商名称`、`发货地址`、`到货地址`、`距离` 和车型价格列识别正确。
6. 选择复跑策略：
   - `只补空白价格`：只处理空白价格，不覆盖已有报价。
   - `全部重跑`：重新读取距离和所有匹配车型报价。
   - `只重跑失败项`：只处理带失败批注的单元格。
7. 点击 `开始`。
8. 浏览器打开货拉拉企业版同城下单页后，人工登录账号。程序不会保存账号密码。
9. 登录并停留在同城下单页后，回到工具点击确认，程序开始批量查询。

第一次打开后，程序会在 `auto_input2.0.exe` 同目录自动生成 `config` 和 `logs` 文件夹。

## 便携文件夹版本

如果下载的是 zip 便携包，解压后：

1. 打开 `启动货拉拉报价工具.bat`，或直接打开 `HuolalaQuoteTool.exe`。
2. 点击 `选择 Excel`，选择要报价的 `.xlsx` 文件。
3. 在 `字段识别预览` 里确认 `供应商名称`、`发货地址`、`到货地址`、`距离` 和车型价格列识别正确。
4. 选择复跑策略：
   - `只补空白价格`：只处理空白价格，不覆盖已有报价。
   - `全部重跑`：重新读取距离和所有匹配车型报价。
   - `只重跑失败项`：只处理带失败批注的单元格。
5. 点击 `开始`。
6. 浏览器打开货拉拉企业版同城下单页后，人工登录账号。程序不会保存账号密码。
7. 登录并停留在同城下单页后，回到工具点击确认，程序开始批量查询。

## 结果文件

工具不会覆盖原 Excel，会在原文件同目录生成：

```text
原文件名_货拉拉报价结果_YYYYMMDD_HHMMSS.xlsx
```

程序每处理完一行都会保存一次。失败原因会写入对应单元格批注。

任务完成后，软件里的 `结果文件` 区域会显示输出文件路径。点击 `打开结果文件`，可以直接用 Excel 查看结果；点击 `打开所在文件夹`，可以定位到结果文件。

如果网页最终参与计价的发货/到货地址和 Excel 里的原地址不完全一致，工具会在对应地址单元格写入 `货拉拉地址不一致` 批注，并在结果文件里新增 `地址不一致汇总` 工作表。汇总表里的地址需要人工确认并修正后再重跑。

## 日志和失败分析

界面下方会实时显示日志，日志文件保存在程序同目录的 `logs` 文件夹。

日志会包含：

- 正在填写哪一行的发货地址和到货地址。
- 网页最终参与计价的发货地址和到货地址。
- 哪些地址与 Excel 不完全一致，需要人工改正。
- 正在切换哪个车长、哪些车型要求。
- 写入的 `运费一口价`。
- 写入的 `总里程`。
- 失败原因分析，例如地址候选弹窗未确认、车型要求残留、网页计价超时、登录失效等。

如果地址识别弹出多个候选，程序会用 Excel 里的供应商名称、发货地址、到货地址、道路和门牌号匹配候选项。候选地址对不上时，该行会标记为地址识别失败，不会继续读取报价。

## 车型规则表

规则表在 `config/vehicle_rules.csv`，可以用 Excel 打开编辑。字段含义：

- `Excel表头关键词`：Excel 里车型报价列的表头关键词，多个写法用英文分号 `;` 分隔。
- `网页车长`：货拉拉网页上要选择的车长，例如 `9米6`。
- `网页车型要求`：货拉拉网页上要选择的车型要求，多个要求用英文分号 `;` 分隔。
- `启用`：填 `是` 启用，填 `否` 跳过。

只有匹配到启用规则的车型价格列会被处理，其他列保留不动。

## 打包

生成单个 EXE：

```powershell
python scripts\build_windows_single_exe.py
```

生成文件：

```text
release\auto_input2.0.exe
```

生成便携文件夹：

在开发电脑上运行：

```powershell
python -m pip install -r requirements.txt
python scripts\build_windows_portable.py
```

生成目录：

```text
dist\HuolalaQuoteToolPortable
```

复制整个文件夹到另一台 Windows 电脑即可运行。工具会优先调用系统自带 Microsoft Edge。
