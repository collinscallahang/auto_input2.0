# 真实网页测试记录

测试页面：

```text
https://bfe-epc-web-v.huolala.cn/#/order-center/same-city?from=menu
```

测试方式：

```powershell
python -X utf8 -u scripts\run_real_sample_smoke.py "D:\codex\auto_input2.0\input\样本 - F4.xlsx" --row 3 --vehicles 3 --no-login-prompt
python -X utf8 -u scripts\run_real_sample_smoke.py "D:\codex\auto_input2.0\input\样本 - F3.xlsx" --row 3 --vehicles 1 --no-login-prompt
```

## 样本 - F4.xlsx

测试行：第 3 行。

- 发货地址：安徽江淮松芝空调有限公司(紫石路)，安徽省合肥市肥西县蓬莱路2869号
- 到货地址：安徽省滁州市南谯区全椒路155号（滁州光启汽车零部件有限公司）
- 总里程：145
- 9.6 车型：真实网页当前返回 `1233.54`
- 12.5&13 车型：真实网页当前返回 `1472.67`
- 16.5&17.5 车型：真实网页当前返回 `1731.53`

说明：程序读取的是页面底部汇总区域的 `运费一口价`，不是优惠后的 `总计`。测试时页面显示货运券抵扣 `-80`，但写回的是抵扣前的一口价。

样本 Excel 中第 3 行 9.6 车型历史值为 `1072.64`。真实网页在本次测试时对同一地址返回 `1233.54`，属于实时价格变化；13 米与 17.5 米返回值与样本历史值一致。

## 样本 - F3.xlsx

测试行：第 3 行。

- 发货地址：安徽江淮松芝空调有限公司(紫石路)，安徽省合肥市肥西县蓬莱路2869号
- 到货地址：安徽省合肥市蜀山区通辽路与机场北路交叉口西南220米（蔚来汽车F3工厂）
- 总里程：47
- 9.6 车型：真实网页当前返回 `817.75`

## 结论

- 能读取样本 Excel 的非固定列位置字段。
- 能按规则识别 9.6、12.5&13、16.5&17.5 三个车型列。
- 能打开货拉拉企业版同城下单页并复用人工登录态。
- 能通过网页 `地址识别` 弹窗填写发货/到货地址。
- 能切换车长和车型要求。
- 能读取 `总里程` 和 `运费一口价`，且不读取优惠后的 `总计`。
